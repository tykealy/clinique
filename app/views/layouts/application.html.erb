<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Clinique" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/bosba.png" type="image/png">
    <link rel="icon" href="/bosba.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/bosba.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= yield %>
    <% if flash[:success] %>
      <%= render 'shared/alert', message: flash[:success] %>
    <% elsif flash[:danger] %>
      <%= render 'shared/alert', message: flash[:danger] %>
    <% end %>
  </body>
</html>
